title: WPFilm_Course
description: Learn to create desktop apps with WPF
template:
  repo: wpfilm_course-template
  name: wpfilm_course
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
